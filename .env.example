POSTGRES_PASSWORD=replace-with-a-long-random-value
GRAFANA_PASSWORD=replace-with-a-long-random-value

# Models are intentionally runtime data rather than image layers.
VISION_BOOTSTRAP_MODELS=0
LLM_BOOTSTRAP_MODELS=0
